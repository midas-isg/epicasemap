include "application.conf"
# DO NOT add any line before this. Insert them below this part.

db.default.url="jdbc:postgresql://localhost:5432/epidemap_dev"
application.context="/epicasemap/" # Play Test Server requires the tailing /